# Browser extension

Track B placeholder. No manifest, permissions, or provider extractors exist yet.
Add them once the first provider extraction method and transfer contract are
validated. Shared import bundle contracts belong to `@quixi/core/contracts`.

The extension extracts and transfers provider-native records. It must not import
the product UI, own canonical schema/migrations, normalize history, or write SQL.
Provider-specific permissions and terms review precede shipping an extractor.
