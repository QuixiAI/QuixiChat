# Product benchmarks

Add reproducible workloads and recorded environments for large archive import,
FTS/hybrid retrieval, compressed-vector recall, migration/export/restore, and PDF
memory/backpressure. Keep synthetic corpus generators small; large generated
corpora and model artifacts stay outside Git.

QuixiEmbed kernel and inference benchmarks live in that package's `perf/`.
Historical Gemma optimization notes remain in `legacy/prototype/docs/`.
